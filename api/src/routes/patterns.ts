import type { FastifyPluginAsync } from 'fastify';

const patternsRoutes: FastifyPluginAsync = async () => {
  // GET /patterns, GET /patterns/:id, CRUD, yardage-check — next iteration
};

export default patternsRoutes;
