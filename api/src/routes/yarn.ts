import type { FastifyPluginAsync } from 'fastify';

const yarnRoutes: FastifyPluginAsync = async () => {
  // GET /yarn, GET /yarn/:id, POST|PATCH|DELETE — next iteration
};

export default yarnRoutes;
