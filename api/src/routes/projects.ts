import type { FastifyPluginAsync } from 'fastify';

const projectsRoutes: FastifyPluginAsync = async () => {
  // GET /projects, GET /projects/:id with joins, CRUD — next iteration
};

export default projectsRoutes;
